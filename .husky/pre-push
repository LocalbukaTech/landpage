
echo "Checking code quality before pushing..."

# 1. Run eslint manually on the whole project (or src directory)
yarn lint

# 2. Run the Next.js production build to catch strict TypeScript/build errors
yarn build